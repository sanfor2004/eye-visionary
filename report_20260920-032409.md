# Eye Visionary Run Report

**Timestamp:** 2026-09-20 03:24:09 (Africa/Cairo)  
**Repository:** `C:\www\eye-visionary`  
**Python:** 3.14.7

## Summary

The implemented local code and test suite ran successfully. Fixture generation, Python compilation, provider imports, and automated tests passed. The model-cache verification correctly reported that the large ML model weights have not yet been downloaded.

## Commands and results

### Deterministic fixture generation

Command:

```text
python scripts/create_synthetic_fixture.py
```

Result: **PASS** — generated `assets/synthetic_two_people.png`.

### Python compilation

Command:

```text
python -m compileall -q eye_visionary scripts
```

Result: **PASS** — no syntax errors.

### Automated tests

Command:

```text
python -m pytest -q
```

Result: **PASS** — `2 passed in 0.01s`.

Covered checks:

- Model manifest points to Florence-2-large and InsightFace buffalo_l.
- Provider modules import without downloading weights.
- Providers can be configured for offline mode.

### Provider smoke test

Command:

```text
python -c "from eye_visionary.models.providers import LocalCaptioner, LocalFaceAnalyzer; c=LocalCaptioner(offline=True); f=LocalFaceAnalyzer(offline=True); print('providers imported'); print('captioner offline=', c.offline, 'face offline=', f.offline)"
```

Result: **PASS**

```text
providers imported
captioner offline= True face offline= True
```

### Model cache verification

Command:

```text
python scripts/verify_models.py --cache-dir .missing-model-cache
```

Result: **EXPECTED BLOCKED CHECK** — no local model cache exists yet.

```text
Missing model paths:
.missing-model-cache\huggingface (Hugging Face cache for microsoft/Florence-2-large)
.missing-model-cache\insightface\models\buffalo_l
Run: python scripts/download_models.py
```

This is not an application failure. It confirms the verifier detects missing offline prerequisites instead of attempting an unplanned network download.

## Fixtures

| Fixture | Dimensions | SHA-256 | Purpose |
|---|---:|---|---|
| `assets/synthetic_two_people.png` | 800x500 | `4275d86c7eb60487f16b2a2b889f88380a7a54c587b589b40953edbd3fb8c072` | Deterministic hashing, metadata, and mocked-provider tests |
| `assets/two_people_generated.png` | 1254x1254 | `7bce302fd25ee1df0a7503132152ee8ab29604c71a3b957316219664b7c5ab95` | Realistic two-face model integration fixture |

## Model status

The repository contains the model manifest, downloader, verifier, and lazy-loading providers. Actual Florence-2-large and InsightFace buffalo_l weights were not downloaded during this run because they are large external artifacts and are intentionally excluded from Git.

To complete a real model inference run while online:

```text
pip install -e ".[models,test]"
python scripts/download_models.py
python scripts/verify_models.py
```

Then set `EYE_VISIONARY_OFFLINE=true` and run the application/provider integration test. InsightFace model licensing must be reviewed before commercial deployment.

## Overall status

**PASS for repository code, fixtures, and automated tests.**  
**Pending for full AI inference:** model weights must be downloaded into the local cache first.
